<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        /* Estilos generales */
body {
    font-family: 'Poppins', sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    text-align: center;
}

/* Encabezado */
header h1 {
    font-size: 2.5rem;
    margin-bottom: 10px;
}

/* Contenido principal */
main p {
    font-size: 1.2rem;
    margin-bottom: 20px;
}

/* Botón */
main a {
    display: inline-block;
    background: white;
    color: #764ba2;
    padding: 12px 20px;
    border-radius: 8px;
    text-decoration: none;
    font-size: 1.2rem;
    font-weight: bold;
    transition: all 0.3s ease;
}

main a:hover {
    background: #f4f4ff;
    color: #5a67d8;
    transform: scale(1.05);
}

/* Footer */
footer {
    position: absolute;
    bottom: 10px;
    font-size: 0.9rem;
}

    </style>
</head>
<body>
    <header>
        <h1>Bienvenido</h1>
    </header>
    
    <main>
        <p>Explora nuestra página y descubre lo que tenemos para ti.</p>
        <a href="login">Empezar</a>
    </main>
    
    <footer>
        <p>&copy; 2024 Mi Página | Todos los derechos reservados</p>
    </footer>
</body>
</html>